{
  "name": "thevocal-kruair",
  "private": true,
  "version": "0.1.0",
  "scripts": {
    "dev": "node scripts/dev.mjs",
    "dev:backend": "npm run dev --prefix backend",
    "dev:frontend": "npm run dev --prefix frontend",
    "setup": "npm run setup --prefix backend"
  },
  "devDependencies": {
    "concurrently": "^9.2.1"
  }
}
